# 開発ログ — Tozawa_NMCP_Cusor

## プロジェクト概要
Unity Natural MCP (`jp.notargs.unity-natural-mcp`) パッケージを利用し、カスタムMCPツールを開発するプロジェクト。
Claude CodeからUnityエディタを操作するためのMCPツール群を実装している。

## プロジェクトパス
`D:\Unity\TozawaUnity\Tozawa_NMCP_Cusor`

## ファイル構成

### カスタムMCPツール（`Assets/Editor/LogToUnityConsole/`）
| ファイル | 説明 |
|---|---|
| `InspectorTool.cs` | Inspector操作の統合ツール（9メソッド） |
| `LogToUnityConsoleTool.cs` | コンソールログ出力（Log/Warning/Error） |
| `CreateEmptyGameObjectTool.cs` | 空GameObjectの作成 |
| `RefreshAssetsTool.cs` | AssetDatabase.Refresh実行 |
| `CustomMcpToolBuilder.cs` | `[McpServerToolType]`属性付きクラスを自動検出・登録するビルダー |
| `TozawaMcpTools.Editor.asmdef` | Editorアセンブリ定義 |

### その他
| ファイル | 説明 |
|---|---|
| `Assets/Editor/McpLogBridge.cs` | ファイルベースのログブリッジ（Temp経由） |

## InspectorTool 設計

### アーキテクチャ
1ファイル（`InspectorTool.cs`）に全てをクラス内クラスとしてまとめたファサードパターン。

```
InspectorTool (MCPメソッド群 = ファサード)
├── GameObjectResolver (パス/InstanceIDからGameObject解決)
├── ComponentResolver (型名/インデックスからComponent解決 + 型解決)
├── PropertyAccessor (SerializedPropertyの読み書き)
├── HierarchyNodeInfo (ヒエラルキー表示)
├── GameObjectInfo (GameObject詳細情報)
├── ComponentInfo (コンポーネント詳細情報)
└── PropertySummary (プロパティ要約情報)
```

### MCPメソッド一覧（全て`Ins_`プレフィックス付き）

| メソッド | 区分 | 説明 |
|---|---|---|
| `Ins_GetHierarchyInfo` | 読み取り | シーンのヒエラルキーツリー取得（深さ指定可） |
| `Ins_GetGameObjectInfo` | 読み取り | GameObjectの詳細情報（タグ、レイヤー、コンポーネント、子要素） |
| `Ins_GetComponentProperties` | 読み取り | コンポーネントのSerializedProperty一覧（パス・型・値） |
| `Ins_SetActive` | 書き込み | GameObjectのアクティブ状態切替（Undo対応） |
| `Ins_RenameGameObject` | 書き込み | GameObjectのリネーム（Undo対応） |
| `Ins_SetPropertyValue` | 書き込み | SerializedPropertyへの値書き込み（Undo対応） |
| `Ins_AddComponent` | コンポーネント操作 | コンポーネント追加（Undo対応） |
| `Ins_RemoveComponent` | コンポーネント操作 | コンポーネント削除（Transform削除禁止、Undo対応） |
| `Ins_SetComponentEnabled` | コンポーネント操作 | コンポーネントの有効/無効切替（Undo対応） |

### `Ins_` プレフィックスの理由
unity-natural-mcpパッケージの組み込みツールと同名メソッドが衝突するため、カスタムツールには`Ins_`プレフィックスを付与。

### SetPropertyValue 対応型
int, bool, float, string, Enum, Color, Vector2/3/4, Vector2Int/3Int, LayerMask

## 開発ログ

### 2026-02-10: カスタムMCPツール基盤整備
- `CustomMcpToolBuilder.cs` による `[McpServerToolType]` 属性の自動検出・登録の仕組みを確認
- 新しいツール追加時に `CustomMcpToolBuilder.cs` の変更は不要であることを確認
- `CreateEmptyGameObjectTool` の動作テスト: MCPツール経由で `MCP_TestObject` を作成し正常動作を確認
- デフォルト `McpUnityEditorTool.RefreshAssets` が機能不全のため、パッケージ側から削除
  - **注意**: パッケージ更新時に復活する可能性あり。その際は再度削除が必要
- カスタム `RefreshAssetsTool` を作成（`AssetDatabase.Refresh(ImportAssetOptions.ForceUpdate)` で強制リフレッシュ）
- ドキュメント整備: `C:\Users\nakaj\.claude\CUSTOM_MCP_TOOLS.md` 作成、グローバル CLAUDE.md にルール追記

### 2026-02-10: InspectorTool 実装完了
- Step 1: 基盤（GameObjectResolver）+ 読み取り系（GetHierarchyInfo, GetGameObjectInfo）→ コンパイルOK
- Step 2: コンポーネント読み取り（ComponentResolver, PropertyAccessor, GetComponentProperties）→ コンパイルOK
- Step 3: 書き込み系（SetActive, RenameGameObject, SetPropertyValue）→ コンパイルOK
- Step 4: コンポーネント操作（AddComponent, RemoveComponent, SetComponentEnabled）→ コンパイルOK
- 組み込みツールとの名前衝突を解消するため `Ins_` プレフィックスに全メソッドをリネーム → コンパイルOK
- MCPサーバー登録確認済み（19ツール = 組み込み10 + カスタム9）

### 2026-02-10: InspectorTool 動作テスト完了
- 全9メソッドの動作テストを実施し、全て正常動作を確認
  - 読み取り系: Ins_GetHierarchyInfo, Ins_GetGameObjectInfo, Ins_GetComponentProperties
  - 書き込み系: Ins_SetActive, Ins_RenameGameObject, Ins_SetPropertyValue（Vector3, Color, bool）
  - コンポーネント操作: Ins_AddComponent, Ins_SetComponentEnabled, Ins_RemoveComponent
- テスト後のシーン状態復元を確認済み
